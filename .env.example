PORT=3000

# PostgreSQL (Локальный сервер или удаленный VDS/VPS)
# Вариант 1: Единая строка подключения (URL)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vesha

# Вариант 2: Отдельные переменные (раскомментируйте, если не используете DATABASE_URL)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGPASSWORD=your_password
# PGDATABASE=vesha
# DATABASE_SSL=false

SESSION_SECRET=change-me-to-a-long-random-string

# Vision / LLM picker (Gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# Если VDS в стране, где Gemini API закрыт (User location is not supported):
# GEMINI_HTTPS_PROXY=http://user:pass@host:port
# OPENAI_API_KEY=  — запасной путь: Whisper + GPT
# SUMMARIZE_MOCK=1
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# YandexGPT (блок llm-picker). Ключ можно переопределить в UI.
# Folder ID каталога облака остаётся на сервере.
YANDEX_API_KEY=
YANDEX_FOLDER_ID=
YANDEX_GPT_MODEL=yandexgpt-lite/latest

# Pandoc CLI for notes-export. Код берёт переменную текущей ОС.
# Пусто = искать pandoc в PATH и в bin/.
PANDOC_PATH_WINDOWS=C:\Program Files\Pandoc\pandoc.exe
PANDOC_PATH_LINUX=/usr/bin/pandoc
# Необязательный запасной путь, если OS-специфичные пусты:
# PANDOC_PATH=

# Search (Yandex via SerpAPI). Without key — demo offers.
SERPAPI_API_KEY=

# Replicate (background removal). Alt model: 851-labs/background-remover
REPLICATE_API_TOKEN=
REPLICATE_REMOVE_BG_MODEL=lucataco/remove-bg

# Google OAuth (optional)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3000/api/auth/google/callback

# yt-dlp + ffmpeg (npm run media-bins → bin/)
MEDIA_BIN_DIR=
YTDLP_PATH=
FFMPEG_PATH=
FFPROBE_PATH=
SUMMARIZE_MAX_DURATION_SEC=10800
SUMMARIZE_TIMEOUT_MS=600000
SUMMARIZE_MAX_FILESIZE=80M

GUEST_UPLOADS_PER_DAY=3
USER_UPLOADS_PER_DAY=30
GUEST_OFFER_LIMIT=5
USER_OFFER_LIMIT=30
